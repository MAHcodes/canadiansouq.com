import styled from "styled-components"

const Hero = () => {
  return (
    <StyledHero>
      Hero
    </StyledHero>
  )
}

const StyledHero = styled.section`
  min-height: 1000px;
`

export default Hero
